apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spark-engine-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: azurefile-sc  # or default if no Azure File configured
