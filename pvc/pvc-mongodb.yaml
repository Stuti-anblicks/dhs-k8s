apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb_data_container-pvc
  namespace: data-harmony
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: azurefile
  resources:
      requests:
        storage: 1Gi